# FastAPI base URL. The connector appends /conversations/message.
BACKEND_API_URL=http://localhost:8000/api/v1
BACKEND_API_TOKEN=

# Dify is called by FastAPI. These values are reserved for deployments that
# inject all AI integration settings from one environment file.
DIFY_BASE_URL=https://api.dify.ai/v1
DIFY_API_KEY=

PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info

# LocalAuth / multi-account defaults
# Restore stored formal sessions on startup. The historical/default session is
# deliberately excluded unless WHATSAPP_AUTO_RESTORE_DEFAULT_SESSION is true.
WHATSAPP_AUTO_RESTORE_SESSIONS=true
WHATSAPP_AUTO_RESTORE_DEFAULT_SESSION=false
WHATSAPP_SESSIONS_PATH=./sessions
WHATSAPP_WEB_VERSION_CACHE_PATH=.wwebjs_cache
WHATSAPP_ACCEPT_GROUP_MESSAGES=false
WHATSAPP_READY_TIMEOUT_MS=120000
WHATSAPP_READY_RETRY_LIMIT=1
WHATSAPP_BROWSER_SHUTDOWN_TIMEOUT_MS=10000
WHATSAPP_PROFILE_UNLOCK_TIMEOUT_MS=5000

# Protects /api/whatsapp/send when set. Use the same value in FastAPI as
# WHATSAPP_GATEWAY_TOKEN.
CONNECTOR_API_TOKEN=

# Browser configuration
PUPPETEER_EXECUTABLE_PATH=
PUPPETEER_HEADLESS=true
PUPPETEER_NO_SANDBOX=false

# Optional browser access for a separately hosted frontend.
CORS_ORIGIN=http://localhost:5173
